apiVersion: v1
kind: Service
metadata:
  name: skeuyrigafe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "skeuyrigafe"
    port: 8080
  selector:
    app: skeuyrigafe
